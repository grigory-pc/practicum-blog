hibernate:
  dialect: org.hibernate.dialect.H2Dialect
  show_sql: true

jdbc:
  test:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:shareit;DB_CLOSE_DELAY=-1;OLD_INFORMATION_SCHEMA=TRUE
    username: sa
    password: password